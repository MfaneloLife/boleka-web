# ===================================================================
# Local Development Environment Variables (.env.local.example)
# Copy to .env.local and fill in real values. Never commit real secrets.
# ===================================================================

# --- Public (Client) Firebase Config (required) ---
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your-measurement-id

# --- Server Firebase Admin (required for all server routes) ---
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# Private key: keep quotes; replace literal \n with actual newlines OR escape them as \n
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nMIIEv...snip...\\n-----END PRIVATE KEY-----\\n"
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Disables GCP metadata probing that causes MetadataLookupWarning locally
GCE_METADATA_HOST=disabled

# Optional: force verbose debug logging (remove or set to 'debug')
# FORCE_LOG_LEVEL=debug

# --- Auth ---
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Database (Prisma / SQLite local) ---
DATABASE_URL="file:./dev.db"

# Node environment (do not set to production locally)
NODE_ENV=development
